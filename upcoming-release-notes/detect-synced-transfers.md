---
category: Features
authors: [hubermjonathan]
---

Optionally apply "Make transfer" automatically when bank sync imports both halves of the same transfer, instead of leaving two unrelated transactions to link by hand. Off by default; set the `sync-detect-transfers` preference to enable it

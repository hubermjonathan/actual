---
category: Features
authors: [hubermjonathan]
---

Optionally link both halves of a transfer that bank sync imported separately, instead of leaving two unrelated transactions. Off by default; set the `sync-detect-transfers` preference to enable it
